# Federated Learning Server Dependencies
# For Windows with Python 3.13+ (no build tools needed)
# Uses latest Flower (may not be compatible with 0.18.0 clients)

# PyTorch (includes compatible NumPy)
torch>=2.0.0
torchvision>=0.15.0

# Flower Framework (latest version with pre-built wheels)
flwr>=1.0.0

# Optimization libraries
scipy>=1.7.0
cvxpy>=1.2.0

# Utilities
matplotlib>=3.5.0
pandas>=1.3.0
tqdm>=4.60.0
psutil>=5.9.0
Pillow>=9.0.0

# Flask (for GUI mode)
flask>=2.3.0
Werkzeug>=2.3.0

# Note: This uses latest Flower which may not be compatible with 0.18.0 clients
# For compatibility with Raspberry Pi clients, use Python 3.9-3.11
