# Federated Learning Dependencies
# Server (Laptop/Desktop) - Compatible with Raspberry Pi clients

# Install in this order to avoid build issues

# NumPy (must be <2.0 for torchvision compatibility)
numpy==1.23.5

# PyTorch
torch>=2.0.0
torchvision>=0.15.0

# Flower Framework (must match client version 0.18.0)
flwr==0.18.0

# Optimization libraries (for ADM algorithm)
scipy>=1.7.0
cvxpy>=1.2.0

# Utilities
matplotlib>=3.5.0
pandas>=1.3.0
tqdm>=4.60.0
psutil>=5.9.0
Pillow>=9.0.0

# (Flask removed - not needed for Flower-only setup)
